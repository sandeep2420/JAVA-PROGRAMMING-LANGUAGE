//Writing a program to print at least 3 Students Attendance Report using Arrays in Java.  //                                                                  //Arrays in Java 
  


public class Arrays { 
 
    public static void main(String[] args) { 
 
        // STEP 1: Declare and initialize an array 
        // 'attendance' is an array that stores attendance percentages of students 
        int[] attendance = {70, 80, 90}; 
 
        // STEP 2: Print a heading 
        System.out.println("Student Attendance Report"); 
 
        // STEP 3: Print each student's attendance using array index 
        System.out.println("Student 1 Attendance: " + attendance[0] + "%"); 
        System.out.println("Student 2 Attendance: " + attendance[1] + "%"); 
        System.out.println("Student 3 Attendance: " + attendance[2] + "%"); 
 
        // STEP 4: Calculate total attendance 
        int total = 0; 
 
        // Loop to add all attendance values 
        for (int i = 0; i < attendance.length; i++) { 
            total = total + attendance[i]; 
        } 
 
        // STEP 5: Calculate average attendance 
        int average = total / attendance.length; 
 
        // STEP 6: Print total and average attendance 
        System.out.println("Total Attendance: " + total); 
        System.out.println("Average Attendance: " + average + "%"); 
    } 
} 
 
